#include <stdio.h>

/**
 * main - assign a random number to the variable n to make it executable
 * and print the last digit of the number in variable n
 * Return: Always 0.
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
